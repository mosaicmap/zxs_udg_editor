<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="zxs_udg_editor" default="default" basedir=".">
    <description>Builds, tests, and runs the project zxs_udg_editor.</description>
    <import file="nbproject/build-impl.xml"/>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar:                  JAR building
      run:                      execution of project 
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="zxs_udg_editor-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->
    
    <property name="version" value="0.5_devel"/>
    
    <property name="build.dir" value="build"/>
    <property name="dist.dir" value="dist"/>
    <property name="dist.jar" value="${dist.dir}/${ant.project.name}.jar"/>
    <property name="dist.src.jar" value="${dist.dir}/${ant.project.name}-src.jar"/>
    
    <property name="res4dist.dir" value="res4dist"/>
    <property name="lib.dir" value="lib"/>
    
    <property name="manifest.name" value="manifest.mf"/>
    <!--
        musí být v NetBeans project.properties:
        #
            manifest.file=manifest.mf
            manifest.available=true
        
        nebo překrýt -do-jar-with-manifest
    -->
    <property name="mf.section.version" value="cz/mp/zxs/tools/udg_editor/"/>
    <property name="mf.mainclass" value="cz.mp.zxs.tools.udg_editor.Main"/>

    <!-- dist final zip -->
    <property name="dist_zip.dir" value="dist_zip"/>

    <property name="dist_zip.file" 
            value="${dist_zip.dir}/${ant.project.name}-v${version}.zip"/>
    
    <zipfileset id="dist_zip.fileset" 
            dir="${dist.dir}"
            prefix="${ant.project.name}">
        <exclude name="*.zip" />
    </zipfileset>
    
    <!-- =================================================================== -->
    
    <target name="-manifest">
        <tstamp>
<!--             <format property="now" pattern="yyyy-MM-dd HH:mm:ss" />-->
            <format property="now" pattern="yyyy-MM-dd" />
        </tstamp>
        <manifest file="${manifest.name}">
            <attribute name="Main-Class"
                    value="${mf.mainclass}"/>
                        
            <!-- Class-Path hodnoty odděleny mezerou -->
            <attribute name="Class-Path"
                    value="lib/slf4j-api-1.7.25.jar lib/slf4j-jdk14-1.7.25.jar"/>            
            <!--attribute name="Class-Path"
                value="lib/commons-cli-1.2.jar"/ -->
                
            <section name="${mf.section.version}">
                <attribute name="Specification-Version" 
                        value="${version}"/>         
                <attribute name="Implementation-Version" 
                        value="${now}"/>
            </section>            
        </manifest>     
    </target>
        
    <!-- "hack" - smazání README.TXT, které generuje NetBeans -->
    <target name="-delete-readmetxt">
        <delete file="${dist.dir}/README.TXT"/>
    </target>
    
    <target name="-copy-res-dist">
        <mkdir dir="${res4dist.dir}"/>
        <copy todir="${dist.dir}" overwrite="true">
            <fileset dir="${res4dist.dir}"/>
        </copy>
    </target>
    
    <target name="-copy-lib-to-dist" depends="-copy-res-dist">
        <copy todir="${dist.dir}" overwrite="true">
            <fileset dir="${lib.dir}"/>
        </copy>
    </target>
        
    <!-- dist final zip -->     
    <target name="-zip-dist" depends="compile">
        <delete quiet="true">   <!-- quiet="true" aby nakončilo chybou když ${dist_final.dir} neexistuje --> 
            <fileset dir="${dist_zip.dir}"/>
        </delete>
        <mkdir dir="${dist_zip.dir}"/>
        
        <zip destfile="${dist_zip.file}" update="true">      
            <zipfileset refid="dist_zip.fileset" />
        </zip>
    </target>
        
    <!-- =================================================================== -->
    
    <!--  -->
    <target name="-pre-jar" depends="-manifest">    
    </target>    
        
    <!--  -->
    <target name="-post-jar" depends="-delete-readmetxt, -copy-res-dist, -zip-dist">
    </target>
        
</project>
